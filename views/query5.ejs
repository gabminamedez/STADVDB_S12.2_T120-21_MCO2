<html>

    <head>
        <link rel="stylesheet" href="/css/styles.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

        <title><%= title %></title>
    </head>

    <body>
        <div class="container">
            <center>
                <h1>Get the average availabilities of every room type listed in a certain neighbourhood group.</h1>
                <hr>

                <form method="post" action="/query5">
                    <div class="form-group">
                        <select required name="neighbourhood_group" class="form-control">
                            <option value="" selected disabled hidden>Choose a neighbourhood_group</option>
                            <option value="Brooklyn">Brooklyn</option>
                            <option value="Manhattan">Manhattan</option>
                            <option value="Queens">Queens</option>
                            <option value="Staten Island">Staten Island</option>
                            <option value="Bronx">Bronx</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-default">Submit</button>
                </form>
                <hr>

                <% if(typeof results == 'undefined') { %>
                    <h2 class="init">Configure the filters above and submit!</h2>
                <% } else if (typeof results != 'undefined' && results.length != 0) { %>
                    <h2>Average of listings in neighbourhood_group = <%= neighbourhood_group %></h2>
                    <table class="table">
                        <tr>
                            <th><h3>room_type</h3></th>
                            <th><h3>avg_availability</h3></th>
                        </tr>
                        <% for(var i = 0; i < results.length; i++) { %>
                            <tr>
                                <td class="row"><%= results[i].room_type %></td>
                                <td><%= results[i].avg_availability %></td>
                            </tr>
                        <% } %>
                    </table>
                <% } else { %>
                    <h2>Average of listings in neighbourhood_group = <%= neighbourhood_group %></h2>
                    <h3 class="err">No results found!</h3>
                <% } %>
            </center>
        </div>
    </body>

</html>